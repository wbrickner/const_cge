/// Use sensor data to control the limbs of a robot
#[const_cge::network("nets/walk.cge")]
struct Walk;

fn main() {
  let mut walk = Walk::default();
  let input = 
  walk.evaluate(&input, &mut output);
}